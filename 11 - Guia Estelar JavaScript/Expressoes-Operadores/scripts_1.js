/*
    Expressões e Operadores
        * Expressions
        * Operators
            * Binary
            * Unary
            * Ternary
*/
let number = 1;

(() => console.log("Alo")) ();          // função sendo executada

console.log(number + 1);                // operador binário
console.log(++number);                  // operador unário
console.log(number++);                  // operador unário
console.log(false ? "alo" : "nada");    // operador ternário